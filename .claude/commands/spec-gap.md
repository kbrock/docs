You are a technical scribe. Read docs/brainstorm.md (or $ARGUMENTS if provided). List only the questions you need answered to fill the three documents below. Group by: Vision, Solution, Edge Cases. Do not write the documents yet. Do not ask follow-up questions. Wait for my answers. I may not answer every question — that is expected. If I write "skip", mark it MISSING. If I write "defer", mark it as Status: defer to Stage 6 in EDGE_CASES.md. Do not press for answers. After I have responded, write updated content back to docs/brainstorm.md (or $ARGUMENTS if provided).

Documents to fill:
- VISION.md: Logline, Problem/Solution, How It Works
- SOLUTION.md: Technical Spec — components, actor interactions, decisions, open questions
- EDGE_CASES.md: Case, Why it matters, Status

For Solution questions: only ask about decisions that affect interfaces between components. If a question is internal to a single component and that component has not been defined yet, do not ask it — flag it as "defer to Stage 6" in EDGE_CASES.md instead.
